# 07_PRODUCT_SPEC.md

> 이 문서는 신규로 추가되었다. `06_technical_spec.md`(→ `08_TECHNICAL_SPEC.md`로 개명 권장)의 GitHub 폴더 구조(§22)에 `07_PRODUCT_SPEC.md` 자리가 이미 예정되어 있었으나 실제로는 작성되지 않았던 문서다.
>
> 01~06(구 기술스펙, 향후 08)은 각각 "무엇을 계산/분석하는가"를 정의한다. 이 문서는 "실제 화면(1인용 웹앱)에서 사용자가 어떻게 그 기능들을 넘나드는가"를 정의한다. 즉 01~06/08이 **엔진 스펙**이라면, 07은 **제품/UX 스펙**이다.

---

# 1. 앱의 목적 (사용자 정의)

1인 개인 투자자가 Cloudflare 기반 무료 인프라 위에서 다음 파이프라인을 하나의 웹앱으로 사용한다.

```text
시장 스크리닝 (02)
    ↓ (종목 더블클릭)
재무분석 (03)
    ↓ (기업명 더블클릭)
비즈니스분석 (04, Gemini)   또는   Valuation (05)
```

핵심 원칙:

1. 각 탭은 **독립적으로도** 진입 가능해야 한다 (예: 재무분석 탭에 기업명을 직접 입력해서 조회 가능).
2. 각 탭은 **이전 탭에서 종목을 더블클릭해서** 자동으로 넘어올 수도 있어야 한다.
3. 시장 스크리닝 데이터는 하루 한 번(영업일 기준 21:00 경계, `02` §4 규칙을 그대로 따름) 배치로 생성되어 D1에 저장된다.
4. **비즈니스분석(04)만 예외적으로 DB에 저장하지 않는다.** 나머지(시장 스크리닝, 재무분석, Valuation)는 모두 Cloudflare D1에 저장한다. (이유: Gemini 응답은 재요청 시 매번 재생성 가능하고, 용량이 크고 가변적이라 개인용 무료 D1 한도에서 부담이 크기 때문 — 자세한 배경은 `04_BUSINESS_MOAT_INDUSTRY.md` §0 참고.)
5. D1 용량이 부족해지면 사용자가 화면에서 직접 데이터를 삭제할 수 있어야 한다.

---

# 2. 탭 구조 (V1)

```text
┌─────────────────────────────────────────────────┐
│ [시장 스크리닝] [재무분석] [비즈니스분석] [Valuation] │
└─────────────────────────────────────────────────┘
```

`06_technical_spec.md`(§29)에 정의된 후보종목/포트폴리오/매매기록 탭은 이번 요청 범위에 포함되지 않으며 그대로 V1 로드맵에 남겨둔다. 이 문서는 위 4개 탭의 **연결 방식**만 새로 정의한다.

---

# 3. 탭별 진입점 (Entry Points)

## 3.1 시장 스크리닝 탭

- 진입점: 앱 접속 시 기본 탭.
- 기준일: `02_market_screening.md` §4 규칙 그대로.
  - 영업일 21:00 이후 → 당일 영업일 기준
  - 영업일 21:00 이전, 토/일/공휴일 → 직전 영업일 기준
- 데이터 소스: 매일 배치로 생성되어 D1(`screening_results` 등)에 저장된 값을 조회만 한다 (화면에서 직접 재계산하지 않음 — `02` §6 원칙).
- 동작: 각 Top20 리스트에서 **종목명을 더블클릭하면 재무분석 탭으로 이동**하고, 해당 종목코드를 파라미터로 넘겨 자동 조회한다.

```text
사용자 동작                     결과
──────────────────────────────────────────
Top20 리스트에서 종목 더블클릭   → 재무분석 탭 활성화
                                → 해당 stock_code로 자동 조회
```

## 3.2 재무분석 탭

두 가지 진입 경로를 모두 지원한다.

```text
경로 A: 시장 스크리닝 탭에서 종목 더블클릭
        → stock_code 파라미터를 받아 자동 조회

경로 B: 재무분석 탭에서 사용자가 직접 회사명 입력 후 조회
        → 03_financial_analysis.md §3 종목 식별 절차를 따름
```

- 데이터: `03_financial_analysis.md`에 정의된 대로 DART API에서 조회 → D1에 저장/누적 → 분기/연간 표 + 시계열 차트로 표시.
- 동작: 화면에 표시된 **회사명(또는 종목 헤더 영역)을 더블클릭하면** 다음 두 곳으로 이동할 수 있다.

```text
회사명 더블클릭 → 사용자에게 이동할 곳 선택
                  ├── 비즈니스분석 탭 (04, Gemini 분석)
                  └── Valuation 탭 (05, 기대수익률)
```

  두 탭 모두로 갈 수 있으므로, 더블클릭 시 두 개의 선택지를 보여주는 간단한 메뉴(또는 두 개의 아이콘/버튼)를 제공한다. 단일 더블클릭에 하나의 목적지만 필요하다면 개발 단계에서 우클릭 메뉴 또는 별도 버튼 2개(예: "사업 분석 보기" / "Valuation 보기")로 대체해도 무방하다 — 이 문서는 "더블클릭 한 번으로 두 곳 중 하나를 정해야 한다"는 제약을 강제하지 않는다.

## 3.3 비즈니스분석 탭

두 가지 진입 경로:

```text
경로 A: 재무분석 탭에서 회사명 더블클릭
경로 B: 비즈니스분석 탭에서 사용자가 직접 회사명 입력 후 조회
```

- 데이터: Gemini API를 호출하여 `04_BUSINESS_MOAT_INDUSTRY.md`에 정의된 항목(사업모델/고객/수익구조/가격결정력/경쟁우위/산업구조/경쟁사/리스크/Thesis)을 그 자리에서 생성한다.
- **저장하지 않는다.** 매 조회마다 Gemini API를 새로 호출한다 (상세 정책은 `04` §0 참고).
- 화면에는 로딩 상태(예: "Gemini로 분석 중...")를 표시한다. Gemini 호출은 수 초~수십 초가 걸릴 수 있으므로 비동기 처리와 로딩 UI가 필요하다.

## 3.4 Valuation 탭

두 가지 진입 경로:

```text
경로 A: 재무분석 탭에서 회사명 더블클릭
경로 B: Valuation 탭에서 사용자가 직접 회사명 입력 후 조회
```

- 데이터: `05_VALUATION.md`에 정의된 BPS × ROE 평균 모형으로 기대수익률을 계산한다.
- 계산에 필요한 BPS·ROE는 이미 D1에 저장된 03 재무데이터에서 가져온다 (새로 수집하지 않음 — `05` §3 원칙).
- 계산 결과(미래 BPS, 상승배수, 기대수익률 등)는 D1의 `valuation` 테이블에 저장한다.

---

# 4. 데이터 저장 정책 요약 (전 탭 공통)

| 탭 | Cloudflare D1 저장 여부 | 비고 |
|---|---|---|
| 시장 스크리닝 | 저장함 | 매일 배치 결과 누적 (`02`, `06` §10) |
| 재무분석 | 저장함 | DART 원천 + 정규화 + 파생지표 누적 (`03`) |
| 비즈니스분석 | **저장하지 않음** | 매 요청마다 Gemini 실시간 호출, 결과는 브라우저 세션에만 존재 |
| Valuation | 저장함 | 계산 결과만 저장, 원천은 03을 참조 (`05` §16) |

이 표는 `04_BUSINESS_MOAT_INDUSTRY.md` §0에 있는 정책과 동일하며, 서로 다른 문서를 열어도 항상 같은 내용을 보게 하기 위해 이 문서와 `04`, `06`/`08` 세 곳에 동일하게 명시한다.

---

# 5. DB 삭제 기능 (용량 관리)

Cloudflare D1 무료 한도(용량) 문제에 대응하기 위해, 화면에서 사용자가 직접 저장된 데이터를 삭제할 수 있는 기능을 제공한다.

## 5.1 요구사항

- 각 탭(또는 별도의 "설정/관리" 화면)에 데이터 삭제 버튼을 둔다.
- 삭제는 **탭(데이터 영역) 단위**로 구분해서 제공한다. 전체를 한 번에 지우는 것과, 특정 영역만 지우는 것을 모두 지원한다.

```text
삭제 가능 대상
────────────────────────────
[ ] 시장 스크리닝 결과 (screening_results, market_daily, adjusted_price_daily 등)
[ ] 재무분석 데이터 (raw_financial, financial_quarterly, financial_annual 등) — 종목 단위 선택 가능
[ ] Valuation 결과 (valuation)
[ ] 전체 삭제
```

비즈니스분석은 애초에 D1에 저장되지 않으므로 삭제 대상 목록에 포함되지 않는다.

## 5.2 안전장치

- 삭제는 되돌릴 수 없으므로, 실행 전 확인 모달(예: "정말 삭제하시겠습니까?")을 반드시 거친다.
- 원천 데이터(raw_market, raw_financial 등)를 삭제하면 이후 재조회 시 `03` §6.2 "증분 조회" 로직이 깨지고 전체 기간을 처음부터 다시 수집하게 된다는 점을 삭제 확인 문구에 안내한다.

## 5.3 API (Cloudflare Worker)

`06_technical_spec.md`(§17)의 API 목록에 다음을 추가한다.

```http
DELETE /api/admin/screening       # 시장 스크리닝 데이터 삭제
DELETE /api/admin/financial/{stock_code}   # 특정 종목 재무데이터 삭제 (파라미터 생략 시 전체)
DELETE /api/admin/valuation/{stock_code}   # 특정 종목 Valuation 결과 삭제 (파라미터 생략 시 전체)
DELETE /api/admin/all             # 전체 삭제
```

1인 전용 앱이므로 복잡한 권한 체계 대신 `06` §18의 Cloudflare Access 인증만으로 접근을 제한한다.

---

# 6. 기준일/시각 규칙 (통일)

`02`, `05`, `06`에 서로 다른 시각(21:00 vs 20:00)이 적혀 있던 것을 사용자 요구사항에 맞춰 **21:00**으로 통일했다 (이 스펙 정리 작업의 일부로 `05`, `06`도 함께 수정했다).

```text
영업일 21:00 이후  → 당일 영업일 기준
영업일 21:00 이전  → 직전 영업일 기준
토요일/일요일/공휴일 → 직전 영업일 기준
```

시장 스크리닝의 기준일과 Valuation의 주가 기준일은 항상 이 규칙을 공유한다.

---

# 7. 화면 간 파라미터 전달 (프론트엔드 구현 힌트)

AI(antigravity)가 프론트엔드를 구현할 때 참고할 수 있는 최소 규약이다. 구체적인 구현(SPA 라우팅, URL 쿼리 등)은 개발 단계에서 자유롭게 정하되, 최소한 다음 정보가 탭 간에 전달되어야 한다.

```text
시장 스크리닝 → 재무분석
    전달값: stock_code

재무분석 → 비즈니스분석
    전달값: stock_code, stock_name

재무분석 → Valuation
    전달값: stock_code, stock_name

비즈니스분석 / Valuation (직접 검색 진입)
    전달값: 사용자가 입력한 회사명 → 종목 식별 절차(03 §3과 동일한 방식) 수행 후 stock_code 확정
```

---

# 8. 문서 번호 체계 권장 변경

`06_technical_spec.md` §22(GitHub 폴더 구조)에 이미 다음 번호 체계가 예정되어 있었다.

```text
01_INVESTMENT_PHILOSOPHY.md
02_MARKET_SCREENING.md
03_FINANCIAL_ANALYSIS.md
04_BUSINESS_MOAT_INDUSTRY.md
05_VALUATION.md
06_PORTFOLIO_KELLY.md   ← 아직 작성되지 않음 (V1 범위 밖, 후보종목/포트폴리오/매매기록 스펙)
07_PRODUCT_SPEC.md      ← 이번에 신규 작성
08_TECHNICAL_SPEC.md    ← 기존 06_technical_spec.md를 이 번호로 옮기는 것을 권장
```

현재 업로드된 `06_technical_spec.md`는 내용상 08번에 해당한다. 실제 리포지토리에 반영할 때 파일명을 `08_TECHNICAL_SPEC.md`로 바꾸는 것을 권장한다 (이번 답변에서는 내용만 수정하고 파일명은 그대로 두었으니, 최종 반영 시 이름을 바꿔달라).
